using System;
using System.Collections.Generic;
using System.Configuration;
using System.Data;
using System.Linq;
using System.Threading.Tasks;
using System.Windows;
using System.Windows.Media.Imaging;

namespace QuanLySinhVienThucTap
{
    /// <summary>
    /// Interaction logic for App.xaml
    /// </summary>
    public partial class App : Application
    {
/*        protected override void OnStartup(StartupEventArgs e)
        {
            // Đặt đường dẫn của tệp tin icon ở đây
            string iconPath = ".\\Static\\Images\\unnamed.ico";

            // Kiểm tra xem tệp tin icon có tồn tại không
            if (System.IO.File.Exists(iconPath))
            {
                // Tạo một đối tượng Uri từ đường dẫn tệp tin icon
                Uri iconUri = new Uri(iconPath, UriKind.RelativeOrAbsolute);

                // Đặt icon cho ứng dụng
                Current.MainWindow.Icon = BitmapFrame.Create(iconUri);
            }

            base.OnStartup(e);
        }*/

    }
}
